int main() {
  // return 12312;
  // return +(--!6); // 看起来像个颜文字
  // return (1 + 2 / 2 + 3 * 4) % 7;
  // return (20 != 90) || (-!((1 * 8) && (2 + 0)));
  // return ((1&& 1) || (22 && 1)) && (10 || 0);
  const int x = 1 + 2 + 3 + 4, y = 1 != 2,
            z = (20 != 90) || (-!((1 * 8) && (2 + 0)));
  // int a = 10, n, b = 19 - 1;
  // a = (a + 1 * 30 + 2 / 10 + 3) * 2;
  // a = a + 1;
  // n = a - b;
  return z;
  // return !((-1)+(+2)-(!3)*0) + 1000 * 8000;
  // return 1 >= 2;
}
